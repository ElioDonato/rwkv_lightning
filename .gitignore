__pycache__/
.vscode/
rwkv_sessions.db
rwkv_sessions.db-journal
rwkv_sessions.db-wal
rwkv_sessions.db-shm
ShareGPT_V3_unfiltered_cleaned_split.json
.codex

# secrets / local env
env.sh
service_run.sh

# model weights (downloaded separately, not for git)
models/

# logs and local test artifacts
*.log

infer/rwkv_batch/cuda/third_party/cutlass/*

# local agent worktrees (not project source)
.qwen-wt/

# local secrets + per-machine env (RWKV_API_PASSWORD for the local test server)
.env

# internal GPU implementation plan (kept local)
IMPL_PLAN_GPU_THROUGHPUT.md
